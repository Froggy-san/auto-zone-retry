import Stripe from "@components/stripe/stripe";
import React from "react";

const Page = () => {
  return (
    <main className=" min-h-full px-2">
      <Stripe />
    </main>
  );
};

export default Page;
